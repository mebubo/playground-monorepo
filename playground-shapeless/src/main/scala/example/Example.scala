package example

import merge.{AnyOps, Merger}

object Example {
  def main(args: Array[String]): Unit = {
//    val foobar = Merger.merge("foo", "bar")(AnyOps.pickNonNull)
//    println(foobar)
  }
}
